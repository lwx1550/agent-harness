# REFLECTION · 项目反思报告

## 一、Superpowers 技能的实际效用

### 发挥了最大作用的技能

**Brainstorming：** 这个技能强制我在写代码之前把设计想清楚。如果没有这个环节，我大概率会直接开始写 agent loop，然后发现缺少护栏、没有配置系统、不知道如何测试。Brainstorming 的"分块呈现 + 逐块确认"机制有效防止了"想当然"的设计决策。

**Subagent-Driven Development：** 虽然实际执行过程中我遇到了 subagent 在沙箱环境中运行缓慢的问题，但这个思路本身非常有价值——每个 task 由独立的 agent 完成，不污染上下文，task 之间通过 review 保证质量。在理想环境中（非沙箱、有网络），这种方式应该能显著提高效率。

**Test-Driven Development：** 在 AI 协作场景中，TDD 的价值被放大了。测试不仅验证代码正确性，更是给 AI 的"精确指令"——"写一个函数，使得这个测试通过"比"写一个函数做某某事"要清晰得多。护栏引擎的 6 个测试就是最好的例子。

### 形式大于实质的技能

**Writing-Plans：** 不是说这个技能没用，而是它产出的计划的详细程度与实际的开发效率之间存在边际递减效应。17 个 task 的计划写得很详细，但实际开发中很多细节（如具体的错误处理方式、边界条件）是在实现过程中才发现的，计划无法预先覆盖。Plan 的价值在于"任务分解"和"依赖关系梳理"，而不是"每行代码的预写"。

**Code Review 流程：** 两阶段审查看似严谨，但在单人项目中，尤其是当代码由 AI 生成时，review 变成了一种"形式上的自我检查"。真正有意义的 review 发生在"冷启动验证"——让一个完全不了解上下文的 agent 去读 SPEC 并实现，才能暴露真正的模糊点。

## 二、TDD 在 AI 协作下是阻碍还是放大器？

**是放大器，但前提是测试写得对。**

在护栏引擎的开发中，TDD 表现很好：我先写了 6 个测试，覆盖了"拦截危险命令"、"放行安全命令"、"按类型匹配"、"审批模式"、"空规则集"、"优先级"等场景。然后让 AI 实现，测试一次性通过。

但在 Agent Loop 的开发中，TDD 遇到了挑战：Agent Loop 涉及 LLM 调用、护栏检查、工具执行、结果回灌等多个环节，很难用纯单元测试覆盖。最终的解决方案是使用 MockLLMClient 来模拟 LLM 响应，让测试变得确定。

结论：TDD 在 AI 协作中不是障碍，但它要求开发者有更强的测试设计能力——"写一个让 AI 可以通过的测试"比"写一个测试来验证自己写的代码"要难。

## 三、Subagent 工作流的实际体验

在我的项目中，subagent 工作流遇到了两个实际问题：

1. **沙箱环境限制：** subagent 在沙箱中运行缓慢，且无法访问网络，导致 pip install 等操作失败。这迫使我最终改为直接在主会话中编写代码。

2. **Task 颗粒度：** 计划中的 task 颗粒度（2-5 分钟）在实际开发中偏小。一个 task 从创建文件到写测试到实现到提交，大约需要 10-15 分钟。更合理的颗粒度是"一个独立的功能模块"。

3. **Subagent 的上下文理解：** 当 subagent 需要理解项目整体架构时，仅提供当前 task 的描述是不够的。例如，实现 Agent Loop 的 subagent 需要理解 Guardrail、ToolRegistry、AuditLogger 等模块的接口，而这些信息分散在多个 task 中。

## 四、SPEC / PLAN 质量对实现质量的影响

**一个具体案例：** 在 SPEC 中，我写了"护栏引擎使用 glob 模式匹配"，但没有明确说明匹配的优先级规则（先匹配先生效 vs 最精确匹配先生效）。结果在实现时，AI 选择了最精确匹配先生效的逻辑，而我预期的是先匹配先生效。这个差异导致测试失败，需要修改实现。

**教训：** 对于有歧义的设计决策，SPEC 必须明确写死。即使是一个"看起来很明显"的规则，也要在 SPEC 中写明。

## 五、最有效的 Prompt / Context 策略

**给 subagent 提供"失败测试"作为上下文：** 这是最有效的策略。当我说"写一个函数使得这个测试通过"时，subagent 的输出质量显著高于"写一个函数做某某事"。测试本身就是一个精确的、可执行的规格说明。

**提供完整代码而非文件路径：** 在 task 描述中直接给出需要创建的文件的完整代码，而不是让 subagent 去读文件。这避免了 subagent 因为文件读取错误或路径问题而浪费时间。

## 六、凭据与分发的工程思考

**凭据管理：** 最初我打算只使用 keyring，但在 Windows 沙箱中发现 keyring 需要登录会话才能工作。这迫使我实现了加密文件降级方案。这个教训是：凭据管理不能只依赖一种机制，必须有 fallback。

**分发：** 选择 GitHub Releases 而不是 PyPI 或 Docker 的原因是：对于 CLI 工具来说，GitHub Releases 是最轻量的分发方式，用户可以直接下载二进制文件，不需要安装 Docker 或配置 Python 环境。

## 七、如果重做我会改变什么

1. **先做冷启动验证，再做实现。** 应该在 SPEC 完成后立即进行冷启动验证，而不是等到所有代码写完。这样可以在实现开始前就发现 SPEC 的模糊点。

2. **减少 worktree 数量。** 5 个 worktree 对应 5 个 PR 的工作流在单人项目中过于繁琐。2-3 个 worktree 就足够了，只要 commit 历史是清晰的。

3. **更早地处理凭据问题。** 应该在 Task 1 中就识别出沙箱环境的限制，而不是等到 Task 13 才发现 keyring 不可用。

## 八、对 Superpowers 方法论的批判

Superpowers 的核心理念是"用流程纪律弥补 AI 的不确定性"。这个理念本身是对的，但它做了几个关键假设：

1. **假设开发环境有完整的网络访问。** 在沙箱/离线环境中，subagent 和技能安装都会失败。
2. **假设开发者有足够的时间执行完整的流程。** Brainstorming → Writing-Plans → Worktrees → Subagent → Review → Finish 的完整流程在时间紧张时可能成为负担。
3. **假设开发者有能力写出高质量的测试。** TDD 的前提是"测试先行"，但如果测试本身写得不好，TDD 反而会引导 AI 走向错误的方向。

在我的项目中，假设 1 被打破，导致我不得不调整工作流。这提醒我们：方法论是工具，不是教条。当环境条件不满足时，应该调整方法而不是盲目遵循。
